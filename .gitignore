*.swp
*.js.map

# clasp local settings (holds the script and spreadsheet ids)
.clasp.json

# Local CLI settings (holds the GAS web app /exec URL)
.env

dist/
/data/
node_modules
html/js/bundle.js
html/js/plan.js

# Claude Code personal settings (project settings under .claude/ are tracked)
.claude/settings.local.json
