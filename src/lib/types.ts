export interface Station {
  pref_id: string;
  station_id: string;
  name: string;
  address: string;
  tel: string;
  hours: string;
  uri: string;
  lat: string;
  lng: string;
  mapcode: string;
}