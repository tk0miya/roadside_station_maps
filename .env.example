# GAS Web App (development-plan spreadsheet API) /exec URL, used by `npm run plan`.
# Get it from `clasp deploy` or the Apps Script deployment dialog.
PLAN_API_URL=https://script.google.com/macros/s/xxxxx/exec
